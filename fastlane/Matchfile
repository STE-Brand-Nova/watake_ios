git_url(ENV.fetch("MATCH_GIT_URL", "SET_MATCH_GIT_URL_IN_CI"))
storage_mode("git")
type("appstore")
app_identifier([ENV.fetch("APP_IDENTIFIER", "com.reypryma.watake")])
readonly(ENV["CI"] == "true")

